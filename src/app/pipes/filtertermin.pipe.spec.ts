import { FilterterminPipe } from './filtertermin.pipe';

describe('FilterterminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterterminPipe();
    expect(pipe).toBeTruthy();
  });
});
